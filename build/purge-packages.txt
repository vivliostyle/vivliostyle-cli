# Install-time-only packages, force-purged after the rootfs is assembled with
#   dpkg --purge --force-depends --force-remove-essential --force-remove-protected
# build/audit.ts fails the build if a purgable package (one outside the runtime
# closure) is classified in none of keep-packages.txt / purge-packages.txt /
# purge-packages-late.txt. To re-derive this list, empty it, build, and triage
# what audit.ts reports -- the same loop as build/essential-packages.txt.
#
# Packages are removed from the dependency-graph leaves first regardless of the
# order here; a second pass (purge-packages-late.txt) holds back tools that the
# first pass's postrm scripts still call.

init-system-helpers

# audit keeps these (closure / data) but they are removable:
#   system software-GL -- Chrome uses its own bundled SwiftShader instead.
mesa-libgallium
libllvm19
libz3-4
#   GTK icon set -- surfaces only in GTK dialogs.
adwaita-icon-theme
#   NodeSource pulls python in so node-gyp is available, but ships no compiler;
#   building native addons at "npm install" needs extra packages anyway, so
#   python alone is pointless.
#   see https://github.com/nodejs/node-gyp/blob/v12.3.0/README.md?plain=1#L72
python3
python3-minimal
python3.13
python3.13-minimal

# gnupg. libassuan9 is kept (poppler-utils -> libpoppler -> libgpgmepp6t64 -> libgpgme11t64).
gnupg
gnupg-l10n
gpg
gpg-agent
gpgsm
gpgconf
dirmngr
pinentry-curses
libnpth0t64
libksba8

xdg-utils
wget
gtk-update-icon-cache
libfontenc1
xfonts-utils
xfonts-encodings
dconf-service
dconf-gsettings-backend
libdconf1
libsensors5
libsensors-config
libapparmor1

# PAM, account management and init: nothing runs as a service in the image
libpam-systemd
libpam-modules
libpam-modules-bin
libpam-runtime
libpam0g
passwd
adduser
login.defs
liblastlog2-2
systemd
systemd-sysv
sysvinit-utils
