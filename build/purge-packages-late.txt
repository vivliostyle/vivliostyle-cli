# Second purge pass. Debian lets Essential packages be used by maintainer
# scripts without being declared as dependencies, so under the forced purge a
# few removal scripts in the first pass would lose tools they call. Those tools
# are held back to this pass:
#   - libpam-* postrm calls debconf's Perl frontend (debconf, perl-base)
#   - postrm scripts call awk (mawk)
#   - python3*'s postrm runs the interpreter (libpython3*)
# see https://salsa.debian.org/dbnpolicy/policy/-/blob/debian/4.7.4.1/policy/ch-binary.rst#L330-337

debconf
perl-base
mawk
libpython3-stdlib
libpython3.13-stdlib
libpython3.13-minimal
