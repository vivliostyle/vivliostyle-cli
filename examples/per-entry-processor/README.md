# Per-Entry Processor Example

This example demonstrates how to specify `documentProcessor` and `documentMetadataReader` per entry.

## Usage

```bash
npm install
npm run build
```

## What This Example Shows

- `chapter1.md`: Custom `documentProcessor` that makes all text red
- `chapter2.md`: Custom `documentMetadataReader` that returns a fixed title
