---
"@vivliostyle/cli": minor
---

Support importing theme packages from CSS with npm package names, e.g. `@import '@vivliostyle/theme-base';`. Imports are resolved from installed packages honoring the `exports` field.
