---
"@vivliostyle/cli": minor
---

Add `cmyk.ifUnmappedColorsFound` option (`'warn' | 'error' | 'ignore'`) to control what happens when RGB colors not mapped to CMYK are encountered. `cmyk.warnUnmapped` is deprecated.
