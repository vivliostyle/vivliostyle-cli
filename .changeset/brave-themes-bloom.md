---
"@vivliostyle/cli": minor
---

Add `vivliostyle theme create` and `vivliostyle theme validate` commands, which scaffold a new Vivliostyle Theme package and validate its `package.json`, along with the `createTheme` and `validateTheme` JavaScript APIs.
